#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Load direnv environment if available
if command -v direnv > /dev/null; then
  eval "$(direnv export bash 2>/dev/null || true)"
fi

echo "🔄 Post-rewrite: Updating submodules..."

# Update submodules after rebase/amend
git submodule update --init --recursive --jobs=8

# Update mirror branches
npm run branches:update > /dev/null 2>&1 || {
  echo "⚠️  Mirror branch update failed (non-blocking)"
}

echo "✅ Post-rewrite complete"